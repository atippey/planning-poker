FROM node:20-alpine

WORKDIR /app

COPY package*.json ./

# Install dependencies with legacy peer deps and force reinstall ajv
RUN npm install --legacy-peer-deps && \
    npm install ajv@^8.12.0 --legacy-peer-deps --save-dev

COPY . .

EXPOSE 3000

CMD ["npm", "start"]
